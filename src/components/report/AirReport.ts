import { css, html, LitElement } from 'lit';

export class AirReport extends LitElement {
  static tag = 'air-core';

  static styles = css``;

  render() {
    return html` <div>Report</div> `;
  }
}

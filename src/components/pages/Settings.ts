import { css, html, LitElement } from 'lit';
import { customElement } from 'lit/decorators.js';

@customElement('air-settings')
export class Settings extends LitElement {
  static styles = css``;

  render() {
    return html` <div>Settings</div> `;
  }
}

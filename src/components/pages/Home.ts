import { css, html, LitElement } from 'lit';
import { customElement } from 'lit/decorators.js';

@customElement('air-home')
export class Home extends LitElement {
  static styles = css``;

  render() {
    return html` <div>Home page</div> `;
  }
}

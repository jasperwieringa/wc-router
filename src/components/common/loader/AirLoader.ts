import { css, html, LitElement } from 'lit';

export class AirLoader extends LitElement {
  static tag = 'air-loader';

  static styles = css``;

  render() {
    return html` <div>Loading...</div> `;
  }
}

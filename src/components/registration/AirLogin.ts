import { css, html, LitElement } from 'lit';

export class AirLogin extends LitElement {
  static tag = 'air-login';

  static styles = css``;

  render() {
    return html` <div>Registration</div> `;
  }
}

import { css, html, LitElement } from 'lit';

export class AirProjects extends LitElement {
  static tag = 'air-projects';

  static styles = css``;

  render() {
    return html` <div>Projects</div> `;
  }
}
